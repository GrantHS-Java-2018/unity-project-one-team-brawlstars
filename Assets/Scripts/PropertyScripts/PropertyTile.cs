using System.Collections;
using System.Collections.Generic;
using PropertyScripts;
using UnityEngine;

public class PropertyTile : GameTile
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
